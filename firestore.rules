rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Authentication data is accessed only by the trusted Go server.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
